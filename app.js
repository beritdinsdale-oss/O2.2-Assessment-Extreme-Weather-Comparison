"use strict";const screens=[...document.querySelectorAll('.screen')],steps=[...document.querySelectorAll('.progress-step')],prev=document.querySelector('#previous'),next=document.querySelector('#next'),position=document.querySelector('#position');let current=0,selected=null;const names=['Welcome','Regions','Climate','Seasons','Over time','Wrap-up'];function show(i){current=Math.max(0,Math.min(i,screens.length-1));screens.forEach((s,n)=>s.classList.toggle('active',n===current));steps.forEach((s,n)=>{s.classList.toggle('active',n===current);s.classList.toggle('complete',n<current)});prev.disabled=current===0;next.disabled=current===screens.length-1;position.textContent=`${names[current]} · ${current+1} of ${screens.length}`;window.scrollTo({top:0})}steps.forEach((s,i)=>s.addEventListener('click',()=>show(i)));document.querySelector('.begin').addEventListener('click',()=>show(1));prev.addEventListener('click',()=>show(current-1));next.addEventListener('click',()=>show(current+1));document.querySelectorAll('.drag-card').forEach(card=>{card.addEventListener('click',()=>{document.querySelectorAll('.drag-card.selected').forEach(c=>c.classList.remove('selected'));selected=card;card.classList.add('selected')});card.addEventListener('dragstart',e=>{selected=card;e.dataTransfer.setData('text/plain',card.dataset.id)})});function place(card,zone){if(!card||card.classList.contains('placed'))return;let item=document.createElement('span');item.className='placed-item';item.textContent=card.textContent.trim();item.dataset.answer=card.dataset.answer;zone.appendChild(item);card.classList.add('placed');card.classList.remove('selected');selected=null}document.querySelectorAll('.match-zone').forEach(zone=>{zone.addEventListener('dragover',e=>e.preventDefault());zone.addEventListener('drop',e=>{e.preventDefault();place(document.querySelector(`.drag-card[data-id="${e.dataTransfer.getData('text/plain')}"]`),zone)});zone.addEventListener('click',()=>{if(selected)place(selected,zone)})});document.querySelector('.reset-match').addEventListener('click',()=>{document.querySelectorAll('.placed-item').forEach(i=>i.remove());document.querySelectorAll('.drag-card').forEach(c=>c.classList.remove('placed','selected'));document.querySelector('.match-feedback').textContent='';selected=null});document.querySelector('.check-match').addEventListener('click',()=>{const cards=document.querySelectorAll('.drag-card'),placed=document.querySelectorAll('.placed-item'),fb=document.querySelector('.match-feedback');if(placed.length<cards.length){fb.className='match-feedback incorrect';fb.textContent='Place all four cards before checking.';return}let ok=true;document.querySelectorAll('.match-zone').forEach(z=>z.querySelectorAll('.placed-item').forEach(i=>{if(i.dataset.answer!==z.dataset.zone)ok=false}));fb.className=`match-feedback ${ok?'correct':'incorrect'}`;fb.textContent=ok?'Nice work. You matched each climate mechanism to the correct extreme.':'Not quite. Review the two explanations above and try again.'});document.querySelector('.check-answer').addEventListener('click',()=>{const s=document.querySelector('input[name="final"]:checked'),fb=document.querySelector('.feedback');if(!s){fb.className='feedback incorrect';fb.textContent='Choose an answer before checking.';return}let ok=s.value==='b';fb.className=`feedback ${ok?'correct':'incorrect'}`;fb.textContent=ok?'Yes. Extreme-weather patterns are part of climate, but they vary across region, season, and hazard.':'Not quite. Think about the difference between one event and a pattern observed across many years.'});document.querySelector('.restart').addEventListener('click',()=>location.reload());show(0);
